var = 3123;
_imap = 25;
while(_imap--) 
    while(++_imap)
        print(var);

while(var *= ((_imap)) - (-_imap))
    _imap %= 5;

while   (-var++ >= ++_imap ) {
    print(var);
      000000;  
        }
               