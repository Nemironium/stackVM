    var = 3123;
    _imap = 25;

    if (_imap-- != 0)
        if (++_imap == _imap)
            print(var);
        else
        {
            print(_imap);
        }
        

    if (((_imap)) - (-_imap) == _imap*2) return 00;
    else
        return 1111;
        