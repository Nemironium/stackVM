    var = 3123;
    _imap = 25;

    if (_imap-- != 0)
        if (_imap++ > _imap) 
            print(var);
        else if (-var-- != 0)
        {
            print(var);
        }
        

    if (((_imap)) - (-_imap) == _imap*2) return(00);
    else if (111)
        return (-1111);
        